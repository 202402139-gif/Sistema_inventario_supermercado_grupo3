import { Component, inject } from '@angular/core'; import { Router } from '@angular/router'; import { Auth } from '../../services/auth';
@Component({selector:'app-registro',standalone:false,templateUrl:'./registro.html',styleUrl:'./registro.css'}) export class Registro { auth=inject(Auth);router=inject(Router);nombre='';email='';password='';confirmar='';rol='empleado';error='';ok='';
 registrar(){this.error='';this.ok='';if(this.password!==this.confirmar){this.error='Las contraseñas no coinciden.';return;}this.auth.register({nombre:this.nombre,email:this.email,password:this.password,rol:this.rol}).subscribe({next:()=>{this.ok='Cuenta creada. Ahora puedes iniciar sesión.';setTimeout(()=>this.router.navigate(['/login']),700)},error:e=>this.error=e.error?.mensaje||'No se pudo registrar.'})}}
